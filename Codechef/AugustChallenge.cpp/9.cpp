// non scorable problem

#include <bits/stdc++.h>
using namespace std;

int main() {
	long long t;
	cin >> t;
	while(t--)
	{
	    long long n, m;
	    cin >> n >> m;
	    while(m--)
	    {
	        int a, b;
	        cin >> a >> b;
	        cout << 0 << endl;
	    }
	}
	return 0;
}
